#include <bits/stdc++.h>
#include <unordered_set>
using namespace std;
int new_map(vector<int> a, int value)
{
    int n;
    cin >> n;
    unordered_set<int> map1(n);
    for (int i = 0; i < a.size(); i++)
    {
        cin >> map1.insert(i);
    }
}